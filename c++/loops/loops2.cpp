#include <iostream>
#include<string>

using namespace std;

int main(){
    int divisor;
do{
    cout<<"Please enter a divisor: "<<endl;
    cin>>divisior;
    if(divisor==0){
        cout<<"0 is not valid";
    }
    int dividend;
    cout<<"Please enter a dividend: ",,endl;
    cin>>dividend;}
    while(dividen%divisor>0){
        dividend--;

    }
    cout<<dividend<< "The dividend is the largest startest point that "<<divisor<< " goes into evenly.";
}
